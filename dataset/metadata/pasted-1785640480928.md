------------------------------------------------------------
🔗 CRITICAL: ID-BASED DATA JOINING (MANDATORY)
------------------------------------------------------------

The system MUST correctly join datasets using IDs.

------------------------------------------------------------
📌 ID FORMAT DEFINITIONS
------------------------------------------------------------

- messages.csv:
    image_id → format: "img_XXX"
    voice_note_id → format: "vn_XXX"

- images.csv:
    image_id → format: "img_XXX"

- voice_notes.csv:
    voice_note_id → format: "vn_XXX"

These IDs MATCH EXACTLY and must be used for joins.

------------------------------------------------------------
🔄 REQUIRED JOIN LOGIC
------------------------------------------------------------

For EACH message:

IF image_id is NOT null:
    - Perform exact match:
        images_df[images_df.image_id == message.image_id]
    - Extract full row

IF voice_note_id is NOT null:
    - Perform exact match:
        voice_df[voice_df.voice_note_id == message.voice_note_id]
    - Extract full row

------------------------------------------------------------
⚠️ NO TRANSFORMATIONS ALLOWED
------------------------------------------------------------

- DO NOT modify IDs
- DO NOT strip prefixes ("img_", "vn_")
- DO NOT cast to numbers
- DO NOT approximate match

ONLY exact string match is valid

------------------------------------------------------------
🧠 ENFORCED DATA USAGE
------------------------------------------------------------

IF:
    message.image_id exists

THEN:
    - images.csv MUST return exactly one matching row
    - That row MUST be used in classification

IF:
    message.voice_note_id exists

THEN:
    - voice_notes.csv MUST return exactly one matching row
    - That row MUST be used in classification

------------------------------------------------------------
🛑 HARD FAILURE CONDITIONS
------------------------------------------------------------

If ANY of the following occurs:

- image_id exists but no match found in images.csv
- voice_note_id exists but no match found in voice_notes.csv
- match found but not used

THEN:
    STOP execution and fix implementation

------------------------------------------------------------
📊 DEBUG LOGGING (MANDATORY)
------------------------------------------------------------

For each message log:

- message_id
- image_id
- image_match_found (True/False)
- voice_note_id
- voice_match_found (True/False)

------------------------------------------------------------
💻 IMPLEMENTATION REQUIREMENT
------------------------------------------------------------

You MUST implement:

def fetch_image_data(image_id, images_df):
    return images_df[images_df["image_id"] == image_id]

def fetch_voice_data(voice_note_id, voice_df):
    return voice_df[voice_df["voice_note_id"] == voice_note_id]

These functions MUST be used in pipeline.

------------------------------------------------------------
If image_id or voice_note_id is present but empty after join, treat it as a critical bug and retry data loading.
------------------------------------------------------------
📌 BUILT-IN MEDIA DETECTION REQUIREMENTS (MANDATORY)
------------------------------------------------------------

The system MUST automatically detect whether a message contains:

- Image data
- Voice note data
- Text-only data

------------------------------------------------------------
🔍 DETECTION LOGIC (STRICT RULES)
------------------------------------------------------------

For each row in messages.csv:

1. IMAGE DETECTION

IF:
    image_id is NOT null
    AND image_id is NOT empty
    AND image_id starts with "img_"

THEN:
    message contains an IMAGE

Set:
    has_image = True

ELSE:
    has_image = False

------------------------------------------------------------

2. VOICE NOTE DETECTION

IF:
    voice_note_id is NOT null
    AND voice_note_id is NOT empty
    AND voice_note_id starts with "vn_"

THEN:
    message contains a VOICE NOTE

Set:
    has_voice = True

ELSE:
    has_voice = False

------------------------------------------------------------

3. TEXT DETECTION

IF:
    message_text is not null AND not empty

THEN:
    has_text = True

------------------------------------------------------------
⚙️ PROCESSING RULE (MANDATORY)
------------------------------------------------------------

For EACH message:

IF has_voice == True:
    → MUST fetch and use voice_notes.csv

IF has_image == True:
    → MUST fetch and use images.csv

IF both exist:
    → MUST use BOTH

IF neither exist:
    → use text only

------------------------------------------------------------
🧠 FINAL REPRESENTATION RULE
------------------------------------------------------------

final_representation must be built as:

IF has_voice:
    include voice transcript

IF has_image:
    include image extracted data

IF has_text:
    include message text

------------------------------------------------------------
📊 VALIDATION (VERY IMPORTANT)
------------------------------------------------------------

Before output:

- Count messages where image_id starts with "img_"
- Ensure ALL were processed using images.csv

- Count messages where voice_note_id starts with "vn_"
- Ensure ALL were processed using voice_notes.csv

------------------------------------------------------------
💻 REQUIRED IMPLEMENTATION
------------------------------------------------------------

You MUST implement:

def detect_media(message):
    has_image = str(message.image_id).startswith("img_")
    has_voice = str(message.voice_note_id).startswith("vn_")
    has_text = message.text is not None and message.text != ""
    return has_image, has_voice, has_text

------------------------------------------------------------
If has_image == True and images.csv is not used → STOP and fix

If has_voice == True and voice_notes.csv is not used → STOP and fix

You are a senior AI systems engineer building a production-grade multimodal message intelligence system.

Your task is to build a COMPLETE Python system for intelligent message notification routing.

------------------------------------------------------------
📁 DATASET CONTRACT (STRICT)
------------------------------------------------------------

All files are inside dataset/

Primary:
- messages.csv (input)
- output.csv (target format)
- sample_messages.csv (reference)

Context:
- users.csv
- groups.csv
- group_members.csv
- business_accounts.csv
- user_business_history.csv
- message_history.csv
- message_events.csv
- images.csv
- voice_notes.csv
- daily_notification_summary.csv

------------------------------------------------------------
🎯 OBJECTIVE
------------------------------------------------------------

For EACH message generate:

message_id,action,message_type,reason,confidence,evidence_message_ids

------------------------------------------------------------
📌 MEDIA DETECTION (MANDATORY)
------------------------------------------------------------

Detect media using:

has_image = image_id exists AND startswith "img_"
has_voice = voice_note_id exists AND startswith "vn_"
has_text = text not empty

------------------------------------------------------------
🔗 ID JOINING (STRICT)
------------------------------------------------------------

- image_id matches images.csv.image_id exactly
- voice_note_id matches voice_notes.csv.voice_note_id exactly

NO transformations allowed.

------------------------------------------------------------
🖼️ MULTIMODAL ENRICHMENT (MANDATORY)
------------------------------------------------------------

For each message:

IF has_image:
    - fetch row from images.csv
    - convert ALL fields into text representation

IF has_voice:
    - fetch row from voice_notes.csv
    - use transcript + metadata as primary content

final_representation = combine(
    message_text,
    image_data,
    voice_data
)

ALL classification MUST use final_representation.

------------------------------------------------------------
🧠 PROCESSING PRIORITY
------------------------------------------------------------

1. voice data
2. image data
3. text
4. metadata

------------------------------------------------------------
👤 USER PERSONALIZATION (MANDATORY)
------------------------------------------------------------

Use:
- reply behavior
- dismiss history
- group activity

Generate user_engagement_score

------------------------------------------------------------
⚖️ IMPORTANCE SCORING ENGINE
------------------------------------------------------------

Compute importance_score using:

- urgency signals
- spam signals
- sender trust
- media richness
- user_engagement_score
- notification fatigue

Mapping:

>0.7 → notify  
0.4–0.7 → digest  
<0.4 → mute  

------------------------------------------------------------
🚫 SPAM / SCAM ENFORCEMENT
------------------------------------------------------------

If detected:
- phishing patterns
- repeated messages
- suspicious financial signals

→ FORCE action = mute

------------------------------------------------------------
📊 CONFIDENCE CALCULATION
------------------------------------------------------------

confidence = sum of:

+ keyword strength  
+ media presence  
+ historical match  
+ sender trust  
- ambiguity  

Clamp between 0 and 1.

------------------------------------------------------------
📄 REASON GENERATION (STRICT)
------------------------------------------------------------

Reason must include:

- detected signal (text/image/voice)
- interpretation
- decision logic

No generic explanations.

------------------------------------------------------------
🔍 EVIDENCE ENGINE
------------------------------------------------------------

Use message_history.csv + message_events.csv

Find similar past messages:

- ignored → supports mute
- replied → supports notify

Output:
- semicolon separated IDs
- or "none"

------------------------------------------------------------
📉 NOTIFICATION FATIGUE CONTROL
------------------------------------------------------------

Use daily_notification_summary.csv

If high notification load:
→ downgrade non-critical messages

------------------------------------------------------------
⚙️ REQUIRED FUNCTIONS
------------------------------------------------------------

detect_media(message)
fetch_image_data(image_id)
fetch_voice_data(voice_note_id)
enrich_message(message)
compute_importance(message)
classify_action(score)

------------------------------------------------------------
📊 LOGGING (MANDATORY)
------------------------------------------------------------

For each message log:

- message_id
- has_image
- has_voice
- image_match_found
- voice_match_found
- importance_score
- action

------------------------------------------------------------
🛑 VALIDATION (HARD REQUIREMENT)
------------------------------------------------------------

Before finishing:

- all messages processed
- all media IDs matched and used
- output row count matches input
- no missing fields

If not → fix before completion

------------------------------------------------------------
📁 PROJECT STRUCTURE
------------------------------------------------------------

code/
main.py
data_loader.py
feature_engineering.py
decision_engine.py
evidence_engine.py
utils.py

logs/
dataset/output.csv

------------------------------------------------------------
🔗 GITHUB AUTOMATION
------------------------------------------------------------

- clone repo
- update code
- generate output.csv
- commit and push

------------------------------------------------------------
▶️ EXECUTION
------------------------------------------------------------

python code/main.py

Must run without manual intervention.

------------------------------------------------------------
📤 OUTPUT RULES
------------------------------------------------------------

- EXACT format required
- one row per message
- valid values only
- no duplicates

------------------------------------------------------------
🎯 FINAL GOAL
------------------------------------------------------------

dataset → multimodal intelligence → scoring → decision → output.csv → commit → run

NO shortcuts. NO skipping media. NO partial implementation.

------------------------------------------------------------
------------------------------------------------------------
🔗 MANDATORY GITHUB REPOSITORY INTEGRATION
------------------------------------------------------------

You MUST update and push all code and outputs to the user's GitHub repository.

------------------------------------------------------------
📌 REPOSITORY ACCESS
------------------------------------------------------------

Use the provided repository URL and GITHUB_TOKEN.

Authenticate using:

https://<GITHUB_TOKEN>@github.com/<repo_path>.git

------------------------------------------------------------
📥 REQUIRED STEPS
------------------------------------------------------------

1. Clone the repository:

git clone https://<GITHUB_TOKEN>@github.com/<repo_path>.git

2. Navigate into repo:

cd <repo_name>

3. Create/update project structure:

- code/
- dataset/output.csv
- logs/
- README.md

------------------------------------------------------------
📤 AFTER CODE GENERATION
------------------------------------------------------------

After building system and generating output.csv:

You MUST execute:

git add .
git commit -m "Built multimodal AI message router and generated output"
git push origin main

------------------------------------------------------------
📊 OUTPUT REQUIREMENT
------------------------------------------------------------

Ensure dataset/output.csv is:

- present in repository
- committed
- pushed successfully

------------------------------------------------------------
🛑 FAILURE CONDITION
------------------------------------------------------------

If repository is NOT updated OR output.csv is missing:

→ TASK IS INCOMPLETE

You MUST retry push until successful.

------------------------------------------------------------
🔍 VERIFICATION
------------------------------------------------------------

After push:

- Confirm repository contains updated code
- Confirm dataset/output.csv exists

------------------------------------------------------------
💻 AUTOMATION REQUIREMENT
------------------------------------------------------------

All Git operations must be executed automatically via script or system calls.

NO manual steps allowed.

------------------------------------------------------------
Scan all available files in the dataset.

For each file:
- Detect its type using file extension:
  - Images: .jpg, .png, .jpeg
  - Voice notes: .mp3, .wav

- If extension is unclear, use filename pattern:
  - "img_" → image
  - "vn_" → voice note

- Classify each file as:
  - image
  - voice

- Assign priority:
  - urgent if it contains important content
  - otherwise normal

- Do NOT skip any files.

- Generate a structured CSV file named output.csv with columns:
  message_id, type, file_path, priority

- Ensure file_path matches actual directory structure:
  - images/ for image files
  - voice_notes/ for audio files

- After generating output.csv:
  - Stage all changes
  - Commit with message "Auto-generated output"
  - Push directly to the main branch of the GitHub repository

- If output.csv already exists:
  - Overwrite it with updated data (do not create duplicates)
- Log skipped or unreadable files
- Validate that each file exists before adding to CSV
- Ensure no duplicate message_id
